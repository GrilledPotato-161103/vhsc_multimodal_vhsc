# @package _global_
# Attempt A3: larger head + deeper
defaults:
  - override /model/ekf_net: default
model:
  ekf_net:
    use_aleatoric: true
    aleatoric_hidden_dim: 64
    aleatoric_n_layers: 3
    lambda_aleatoric: 1.0
logger:
  wandb:
    name: "aleatoric_A3_hdim64_l3_${now:%H-%M-%S}"
    tags: ["aleatoric", "A3", "hdim64"]
