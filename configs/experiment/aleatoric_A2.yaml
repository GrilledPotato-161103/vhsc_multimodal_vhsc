# @package _global_
# Attempt A2: medium head (recommended baseline)
defaults:
  - override /model/ekf_net: default
model:
  ekf_net:
    use_aleatoric: true
    aleatoric_hidden_dim: 32
    aleatoric_n_layers: 2
    lambda_aleatoric: 1.0
logger:
  wandb:
    name: "aleatoric_A2_hdim32_${now:%H-%M-%S}"
    tags: ["aleatoric", "A2", "hdim32"]
