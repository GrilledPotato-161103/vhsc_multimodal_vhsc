# @package _global_
# Attempt A1: smallest aleatoric head — can the signal be captured?
defaults:
  - override /model/ekf_net: default
model:
  ekf_net:
    use_aleatoric: true
    aleatoric_hidden_dim: 16
    aleatoric_n_layers: 2
    lambda_aleatoric: 1.0
logger:
  wandb:
    name: "aleatoric_A1_hdim16_${now:%H-%M-%S}"
    tags: ["aleatoric", "A1", "hdim16"]
