# @package _global_
# Attempt B2: lambda=5.0 — aleatoric amplified, fills blobs faster
defaults:
  - override /model/ekf_net: default
model:
  ekf_net:
    use_aleatoric: true
    aleatoric_hidden_dim: 32
    aleatoric_n_layers: 2
    lambda_aleatoric: 5.0
logger:
  wandb:
    name: "aleatoric_B2_lam5_${now:%H-%M-%S}"
    tags: ["aleatoric", "B2", "lam5"]
